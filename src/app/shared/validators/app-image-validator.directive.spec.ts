import { AppImageValidatorDirective } from './app-image-validator.directive';

describe('AppImageValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AppImageValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
